spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    com.travel_events_consumer: INFO
    org.flywaydb: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
